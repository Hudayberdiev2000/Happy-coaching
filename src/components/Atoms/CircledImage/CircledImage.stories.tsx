import CircledImage from './CircledImage';

export const Default = {};

const circledImageConfig = {
  title: 'Atoms/CircledImage',
  component: CircledImage,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default circledImageConfig;

import DarkModeButton from './DarkModeButton';

export const Default = {};

const darkModeButtonConfig = {
  title: 'Atoms/DarkModeButton',
  component: DarkModeButton,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default darkModeButtonConfig;

import Input from './Input';

export const Default = {};

const inputConfig = {
  title: 'Atoms/Input',
  component: Input,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default inputConfig;

import Logo from './Logo';

export const Default = {};

const logoConfig = {
  title: 'Atoms/Logo',
  component: Logo,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default logoConfig;

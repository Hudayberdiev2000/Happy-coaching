import HeroImageBox from './HeroImageBox';

export const Default = {};

const heroImageBoxConfig = {
  title: 'Molecules/HeroImageBox',
  component: HeroImageBox,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default heroImageBoxConfig;

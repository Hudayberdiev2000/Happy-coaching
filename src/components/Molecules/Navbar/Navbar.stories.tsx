import Navbar from './Navbar';

export const Default = {};

const navbarConfig = {
  title: 'Molecules/Navbar',
  component: Navbar,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default navbarConfig;

import Blogs from './Blogs';

export const Default = {};

const blogsConfig = {
  title: 'Organisms/Blogs',
  component: Blogs,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default blogsConfig;

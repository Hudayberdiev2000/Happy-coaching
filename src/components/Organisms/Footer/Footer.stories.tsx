import Footer from './Footer';

export const Default = {};

const footerConfig = {
  title: 'Organisms/Footer',
  component: Footer,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default footerConfig;

import FreeGuide from './FreeGuide';

export const Default = {};

const freeGuideConfig = {
  title: 'Organisms/FreeGuide',
  component: FreeGuide,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default freeGuideConfig;

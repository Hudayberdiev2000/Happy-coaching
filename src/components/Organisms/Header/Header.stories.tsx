import Header from './Header';

export const Default = {};

const headerConfig = {
  title: 'Organisms/Header',
  component: Header,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default headerConfig;

import QualificationSection from './QualificationSection';

export const Default = {};

const qualificationSectionConfig = {
  title: 'Organisms/QualificationSection',
  component: QualificationSection,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default qualificationSectionConfig;

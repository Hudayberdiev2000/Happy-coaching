import FreeGuidePage from './FreeGuidePage';

export const Default = {};

const freeGuidePageConfig = {
  title: 'Pages/FreeGuidePage',
  component: FreeGuidePage,
  tags: ['autodocs'],
  excludeStories: /.*Data$/,
};

export default freeGuidePageConfig;
